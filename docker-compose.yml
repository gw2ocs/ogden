version: '3'

services:
  node:
    build: .
    networks:
      - default
      - gw2trivia_bridge
    volumes:
      - ./bwd:/usr/src/app/src/bwd
      - /usr/src/app/node_modules
    env_file:
      - .env
    command: npm start
networks:
  gw2trivia_bridge:
    external: true
